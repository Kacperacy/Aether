[package]
name = "engine"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"

[dependencies]
aether-core = { workspace = true }
attacks = { workspace = true }
board = { workspace = true }
movegen = { workspace = true }

[lints]
workspace = true

[features]
# Exposes the search parameters as hidden UCI options so an external tuner
# can drive them without recompiling. Off by default; the shipping build folds
# every parameter to a compile-time constant.
tune = []
